# Colab compute deps — grounding model inference + fine-tuning.
# Install after requirements.txt: pip install -r requirements-colab.txt

# Grounding models
# NOTE: JEDI (vllm) and OmniParser (Florence-2) have incompatible transformers
# version requirements. Run them in SEPARATE Colab notebooks:
#   - colab/jedi_smoke.ipynb      -> transformers latest + vllm latest
#   - colab/omniparser_smoke.ipynb -> transformers==4.49.0 + Pillow<11, NO vllm
# This file lists deps needed by one or the other; each notebook installs the
# subset it needs with its own pinned versions.
transformers
accelerate
einops
timm
huggingface_hub
qwen-vl-utils    # JEDI is Qwen2.5-VL based

# Fine-tuning (Approach 2)
peft
bitsandbytes
datasets

# RAG (Approach 3)
faiss-cpu
sentence-transformers
