# Colab compute deps — grounding model inference + fine-tuning.
# Install after requirements.txt: pip install -r requirements-colab.txt

# Grounding models
# transformers pinned to 4.49.0: Microsoft's Florence-2 (used by OmniParser)
# trust_remote_code was written for this version; 4.50+ breaks it. Community
# consensus fix: pin. See investigation in plans/giggly-snuggling-wand.md.
transformers==4.49.0
# vllm 0.8.2 is the highest version compatible with transformers 4.49 AND
# supports Qwen2.5-VL (used by JEDI). 0.8.3+ require transformers>=4.51.
vllm==0.8.2
accelerate
einops
timm
huggingface_hub
qwen-vl-utils    # JEDI is Qwen2.5-VL based

# Fine-tuning (Approach 2)
peft
bitsandbytes
datasets

# RAG (Approach 3)
faiss-cpu
sentence-transformers
